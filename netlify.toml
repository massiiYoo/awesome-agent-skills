# Configurazione Netlify per Anestesia Vault
# Pubblica direttamente l'app (sito statico, nessuna build necessaria).
[build]
  publish = "anestesia-vault"

# Fallback: ogni percorso serve l'app (single-file app).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
